# Заключение по исследованию: USB-хост CH32V317 (USBHS/USBHD + USBFS), устройства за хабом

**Проект:** `D:\src\new_evo\test_usb_1` (PlatformIO, env `genericCH32V317WCU6`)
**Аппаратура:** CH32V317 (USBHS/USBHD + USBFS/OTG\_FS); хабы CH334 (HS, 2 TT) и FE1.1s (Terminus, VID `1a40:0101`)
**Документы:** `CH32FV2x_V3xRM.PDF` (V2.3), даташиты CH334 / FE1.1s
**Дата:** 23.09.2026

---

## 1. Коротко (TL;DR)

| Вопрос | Ответ |
|---|---|
| Енумерация FS-устройств за хабом на **USBHS** | **Работает** (проверено на CH334) |
| Мышь (LS, 1.5 Мбит/с) за хабом на **USBHS** | **Невозможно аппаратно** — у USBHD нет PRE-токена (в отличие от USBFS) |
| Мышь (LS) за хабом на **USBFS** | **Работает**, проверено в этом же проекте (хаб FE1.1s): енумерация + поток HID-отчётов |
| HS-устройства за хабом на USBHS (SPLIT-транзакции) | **Не удалось включить**: механизм не документирован в RM, чёрный ящик (4096 × 4 × 2 попыток) не дал ни одного отклика |
| Крупный побочный результат | Найден и устранён дефект, из-за которого **HID-данные не читались вообще нигде** (глобальные прерывания были запрещены) |
| Финальная конфигурация | Оба порта в одной прошивке; `Flash 10.4% (27348 B)`, сборка без предупреждений, прошито (`Verified OK`) |

---

## 2. Исходная задача

Лог на COM3: хаб CH334 на USBHS енумерировался (`DevType: 09`, `bDeviceProtocol = 02` = HS-хаб, `RootHubDev[01].bPortNum = 04`), но после `Further Enum Result: OK` — **тишина**: устройства на портах хаба не обнаруживались и не енумерировались.

## 3. Что было на старте (диагноз)

* Вся логика 2-го уровня (устройства за хабом) в `src/USB_Host/app_km.c` была закрыта `#if DEF_USBFS_PORT_EN` (= 0 в этом проекте) и прошита жёсткими вызовами `USBFSH_*`; объявления `hub_port`/`hub_dat` тоже были под этим макросом.
* Опрос HID/хабов завязан на `TIM3`-счётчики интервалов (`InEndpTimeCount`), которые инкрементируются в `TIM3_IRQHandler`.

---

## 4. Этап 1. Работоспособность хаба на USBHS — найденные и устранённые дефекты

| № | Симптом | Причина | Исправление | Проверка |
|---|---|---|---|---|
| 1 | **HID-данные не читались нигде**: счётчики интервалов не росли, опроса не было | в `src/startup_ch32v30x_D8C.S` `mstatus = 0x7800` → **бит MIE = 0**, глобальные прерывания запрещены → `TIM3_IRQHandler` не вызывался никогда (флаг `Update` висел: `tif=1`, счётчик `TIM3` при этом тикал) | `__enable_irq()` в `main()` после `TIM3_Init()` (+ печать `Global IRQ Enabled`) | до фикса: `ticks=0`, `cnt=00`; после: ISR тикает, `InEndpTimeCount` растёт, данные пошли |
| 2 | Ветка хаба не компилировалась для USBHS, вызовы уходили в FS-драйвер | логика под `#if DEF_USBFS_PORT_EN` + хардкод `USBFSH_*` | порт-независимые диспетчеры `USBH_SetSelfAddr`, `USBH_SetSelfSpeed`, `USBH_HubDevGetDeviceDescr/SetUsbAddress/GetConfigDescr/SetUsbConfig`; ветка хаба без USBFS-гарда (`bType == USB_DEV_CLASS_HUB`) | устройства за хабом енумерируются на обоих портах |
| 3 | HS-хаб на USBHS: устройства за ним недостижимы | в HS-режиме FS/LS за HS-хабом требуют SPLIT-транзакций | `DEF_USBHS_HUB_FS_MODE`: хаб переенумерируется в FULL speed; добавлен `USBHSH_ResetRootHubPortSpeed(mode, speed)` — скорость ставится **до** `BUS_RESET`, HS-хендшейк не выполняется | `HUB on USBHS: switch port to FULL speed` → `bDeviceProtocol 00` → `Root Dev Speed:1` |
| 4 | **Порт 1 хаба не проверялся никогда** (мышь была именно там) | дефект WCH: маска изменений `1 << hub_port` при 1-based номере порта | `1 << ( hub_port - 1 )` в `HUB_Port_PreEnum1/2`, `1 << hub_port` при маскировании | `Hub Port1 In` → устройство на порту 1 находится |
| 5 | `HUB_PE1_ERR1:2e` (STALL) на портах 3–4 при сканировании | адрес хоста оставался адресом уже енумерированного устройства → запросы к хабу уходили «не туда» | восстановление адреса/скорости хаба на каждой итерации сканирования | сканирование всех портов без STALL |
| 6 | Устройства, вставленные до енумерации хаба, не обнаруживались | нет события изменения порта | однократный скан всех портов после енумерации (`USBH_HubScanAll[]`) | `HUB ports will be scanned`, `Hub Int Data:ff` |
| 7 | Порты CH334 могли не успеть подняться | не было паузы после подачи питания | пауза по `bPwrOn2PwrGood` (CH334 = 96 мс, минимум 100 мс) | стабильная енумерация |
| 8 | LS-устройство за хабом на USBHS «роняло» сам хаб | попытка увести порт в LS (как в USBFS) при отсутствии PRE у USBHD | LS-устройство за хабом определяется (`Dev Speed:0`) и помечается неподдерживаемым, порт остаётся FS | `HUB port1: low-speed device behind the HUB is not supported!`, хаб продолжает работать |
| 9 | После ре-енумерации кэш скорости оставался HS | драйвер кэширует скорость только при «холодном» подключении | `USBH_EnableRootHubPort` перечитывает фактическую `SPEED_TYPE` | корректный `Root Dev Speed` |

Дополнительно добавлена диагностика: `Root Dev Speed:%x`, `Hub Int Data:%02x`, `HubP%x pre1/pre2`, `HUB ports will be scanned`, печать `!!! HardFault !!!` в обработчике (до этого fault был «тихим» из-за `NVIC_SystemReset()`), а также чтение состояния через OpenOCD (PC/регистры/RAM).

---

## 5. Этап 2. НИОКР: HS-режим + SPLIT-транзакции (отрицательный результат с полной доказательной базой)

### 5.1. Поиск документации

| Источник | Результат |
|---|---|
| RM V2.3 (`CH32FV2x_V3xRM.PDF`), глава 22 (USBHD) | Просканирован **целиком**: слово `SPLIT` встречается только на стр. 377 (`RB_UMS_SPLIT_CAN`), 386 (`R16_UH_SPLIT_DATA` в таблице), 389 (`RB_UH_T_DATA_NO` = «PING/SPLIT»), 390 (описание `R16_UH_SPLIT_DATA`: «the lower 12 bits are valid»). **Процедуры, раскладки полей и последовательности SSPLIT/CSPLIT нет.** |
| `openwch/ch32v307` (актуальный EVT) | USBHS host-драйвер **без хаба и без SPLIT** (примеры только `HOST_KM/IAP/Udisk/MTP`); файл совпадает с локальным |
| CherryUSB (порт WCH) | Только **device** (`usb_dc_usbhs.c`), host-порта для WCH нет |
| GitHub issues/код по `UH_SPLIT_DATA` | 0 результатов (issues API); прочие индексы кода недоступны/лимитированы |
| Даташиты хабов (локально) | FE1.1s — HS-хаб («USB 2.0 HIGH SPEED 4-PORT HUB CONTROLLER», split-буферы 32/6, поддержка HS/FS/LS вниз); раскладки SPLIT-пакета в даташите нет (это хост-сторона) |
| WCH-сообщество (`wch.cn/bbs`) | Есть тема/апнота «CH32V307 USB2 主机 接 Hub 读取键鼠、触摸屏», но сайт — JS-SPA, поиск требует аккаунта; получить текст не удалось |

### 5.2. Методика: зонд и калибровка

Сделана отдельная зонд-прошивка (`src/USB_Host/usbhs_hub_probe.c`, примитивы `USBHSH_HubSplitTransact()` / `USBHSH_SplitWaitMicroframe()` в драйвере):
* самостоятельная енумерация хаба в **HS** (без разговора с хабом SPLIT не нужен);
* скорость целевого устройства — из port status хаба (`Dev Speed: full/low/high`);
* **калибровка измерительного тракта** (обычные транзакции к хабу) — доказала, что измерения корректны:

| Транзакция | `INT_ST` | Смысл |
|---|---|---|
| `IN` хаб EP1, ожидание DATA1 | `0x03` | DATA0 |
| `IN` хаб EP1, ожидание DATA0 | `0x0B` | DATA1 |
| `OUT` хаб EP0 | `0x02` | ACK |
| `IN` к устройству за хабом **без split** | `0x00` | **нет ответа/таймаут** (`MASK_UIS_H_RES[3:0] = 0`, RM стр. 379: «0000 means the device has no response or timed out») |

### 5.3. Что проверено и что дало ноль

| Проверка | Объём | Результат |
|---|---|---|
| `R16_UH_SPLIT_DATA` = 0…0xFFF × 4 комбинации фаз `DATA_NO` (0/1 для SSPLIT и CSPLIT) × 2 режима `R16_USB_DEV_AD` (адрес устройства / **адрес хаба**) | 32 768 попыток | **0 откликов** от устройства за хабом. При `DEV_AD` = адрес хаба хаб отвечает сам (4096/4096) — то есть железо делает **обычную** транзакцию к хабу, SPLIT не генерируется |
| Токен-пространство: PID `SPLIT` (0x8) и `PING` (0x4) × `DATA_NO` × `DEV_AD` | 65 536 попыток | ответы не зависят от payload (мусорные), рабочего split нет |
| Гейт по `R8_USB_MIS_ST.RB_UMS_SPLIT_CAN` перед каждой фазой | — | бит **динамический** (`ready=1099 / busy=901` за 40 мс, ~50/50); гейт ничего не изменил |
| Проверка ложных срабатываний | — | первое «нашлось» (`SPLIT FOUND a=1 v=000`) оказалось дескриптором **самого хаба** (VID `1a86:8091`); детектор исправлен (сравнение с дескриптором хаба), после чего `SPLIT TARGET hits: 0` |
| `SPEED_TYPE` = HS, `UMS_SPLIT_CAN` = 1 | — | в HS-режиме железо сообщает, что SPLIT «разрешён» (но не генерирует его) |

### 5.4. Вывод

Сгенерировать SPLIT-транзакцию на этом кремнии «снаружи» не удалось даже при полном переборе задокументированных регистров и их комбинаций, при доказанно рабочем измерительном тракте. Bез апноты/поддержки WCH (раскладка 12 бит `R16_UH_SPLIT_DATA`, последовательность SSPLIT/CSPLIT, роль `DATA_NO` и `SPLIT_CAN`) продвижение возможно только гаданием. Непроверенными остались маловероятные ветки: паузы на границах микрофреймов, варианты `UC_SPEED_TYPE` (FULL/LOW при HS-линке) и `UH_EP_TYPE`, а также недокументированный смысл «динамического» `SPLIT_CAN`.

**Практический вывод:** если LS/FS-устройства за хабом нужны — использовать порт **USBFS** (там PRE-токен есть) либо подключать LS напрямую в USBHS.

---

## 6. Этап 3. USBFS + FE1.1s: LS-мышь за хабом — работает

Включён `DEF_USBFS_PORT_EN 1` — оба порта в одной прошивке. Проверено на железе:

| Порт | Что подключено | Результат |
|---|---|---|
| USBFS (0) | хаб FE1.1s (VID `1a40:0101`, «USB 2.0 Hub», 4 порта, `bPwrOn2PwrGood = 0x32`), порт 4 | **LS-мышь** `18f8:0f99` «USB OPTICAL MOUSE» (2 интерфейса HID): `Dev Speed:0` → дескриптор → `DevType: 03` → `HUB port4 device is HID! Further Enum: … OK!` → **поток HID-отчётов** (`01 00 00 00 00 00`, …) |
| USBFS (0) | тот же хаб, порт 2 | USB 1.1-устройство `23a0:0008` (класс 0x0b) — енумерировано |
| USBHS (1) | хаб CH334 | HS→FS переключение, `Root Dev Speed:1`; порт 1 — LS (корректно отсечено), порт 2 — FS-устройство енумерировано |
| — | после загрузки | в логе **нет ни одной строки `Err(` / `Enum Err`**; 2 первичных скана + HID-отчёты (12 строк за 20 с наблюдения) |

### 6.1. Найденный и исправленный регресс (важно)

При включении USBFS всплыла ошибка, внесённая уже в ходе этой работы: в `USBH_SetSelfSpeed()` сброс бита `USBFS_UH_LOW_SPEED` (`HOST_CTRL`) выполнялся по скорости **устройства** вместо скорости корневого линка. Для LS-мыши за хабом это переводило **весь** FS-порт в LS → хаб становился неуправляемым, и всё сыпалось `Err(20)` (включая устройство на USBHS). Возвращена исходная семантика WCH:

```c
USBFSH_SetSelfSpeed( speed );                 /* включает PRE-токен (HOST_SETUP.PRE_PID_EN) */
if( ( speed != USB_LOW_SPEED ) || ( RootHubDev[ usb_port ].bSpeed != USB_LOW_SPEED ) )
    USBFSH->HOST_CTRL &= ~USBFS_UH_LOW_SPEED; /* порт остаётся FS — иначе хаб недостижим */
```

Вывод: та «странная» строка в оригинальном коде WCH была осознанно правильной: PRE ставится, а бит LS корневого порта сбрасывается; для LS-устройства напрямую (root = LOW) — сохраняется.

---

## 7. Итоговая матрица возможностей (проверено)

| Устройство | USBFS + хаб | USBHS + хаб |
|---|---|---|
| HS / FS | ✔ | ✔ (порт переводится в FS; HS-устройства за хабом деградируют до FS) |
| **LS (мышь)** | ✔ **работает** (PRE, проверено) | ✘ аппаратно невозможно (у USBHD нет PRE) |
| LS напрямую в порт | ✔ | ✔ |
| HS за хабом на USBHS | н/д | ✘ требует SPLIT (этап 2 — закрыт с отрицательным результатом) |

---

## 8. Артефакты, файлы и переключатели

Финальное состояние `src/USB_Host/usb_host_config.h`:

| Макрос | Значение | Смысл |
|---|---|---|
| `DEF_TOTAL_ROOT_HUB` | 2 | два корневых порта (USBFS + USBHS) |
| `DEF_USBFS_PORT_EN` | 1 | порт USBFS включён (0 — только USBHS) |
| `DEF_USBHS_PORT_EN` | 1 | порт USBHS включён |
| `DEF_USBFS_PORT_INDEX` / `DEF_USBHS_PORT_INDEX` | 0 / 1 | индексы в `RootHubDev[]` / `HostCtl[]` |
| `DEF_USBHS_HUB_FS_MODE` | 1 | хаб на USBHS переенумерируется в FS (SPLIT не нужен); 0 — HS-линк + эксперимент |
| `DEF_USBHS_HUB_SPLIT_PROBE` | 0 | экспериментальный зонд SPLIT (требует `DEF_USBHS_HUB_FS_MODE 0`) |
| `DEF_USBHS_HUB_PROBE_PORT` | 2 | порт хаба для зонда (1-based) |
| `DEF_DEBUG_PRINTF` | 1 | общая отладочная печать |
| `DEF_DEBUG_HUB_SCAN` | 1 | детали сканирования портов (`HubP%x pre1/pre2`, `HUB ports will be scanned`) |
| `DEF_DEBUG_HID_REPORT` | 1 | дамп каждого HID-отчёта (0 — «тихий» лог, сборка 26964 B) |

Изменённые / новые файлы:

| Файл | Что сделано |
|---|---|
| `src/main.c` | `__enable_irq()` (+`Global IRQ Enabled`), вызов зонда по макросу, касты аргументов `printf` (сборка без предупреждений) |
| `src/ch32v30x_it.c` | печать `!!! HardFault !!!` перед `NVIC_SystemReset()` |
| `src/USB_Host/app_km.c` | порт-независимая логика хаба (1639/1665/1714/1745/1778/1809), HS→FS (648), LS-обработка (2366), фиксы WCH (порт 1 → 2325/2343, адрес хаба → 2312, питание), перечитывание скорости (237), диагностика (688/2124/2205/2460) |
| `src/USB_Host/ch32v30x_usbhs_host.c/.h` | `USBHSH_ResetRootHubPortSpeed` (212), `USBHSH_SplitWaitMicroframe` (401), `USBHSH_HubSplitTransact` (428) |
| `src/USB_Host/usbhs_hub_probe.c/.h` | экспериментальный зонд SPLIT (~16 КБ, выключен макросом) |
| `src/USB_Host/usb_host_config.h` | все переключатели + новые debug-макросы |

## 9. Диагностика: что означают строки лога

| Строка | Смысл |
|---|---|
| `Global IRQ Enabled` | глобальные прерывания разрешены (обязательно для опроса HID/хабов) |
| `HUB on USBHS: switch port to FULL speed` | хаб на USBHS переенумерируется в FS (SPLIT не потребуется) |
| `Root Dev Speed:x` | 0 = LS, 1 = FS, 2 = HS (фактическая скорость корневого устройства) |
| `Hub Int Data:%02x` | битовая карта изменений портов хаба (0-based: бит 0 = порт 1) |
| `HubP%x pre1/pre2` | результаты PreEnum-шагов для порта (`fe` = нет события, `15` = CONNECT) |
| `Dev Speed:x` | скорость устройства на порту хаба (0 = LS → за хабом на USBHS не поддерживается) |
| `HUB port%x: low-speed device behind the HUB is not supported!` | LS-устройство за хабом на USBHS (нет PRE); хаб остаётся рабочим |
| `HUB port%x device is …` | устройство за хабом енумерировано (класс: HID/unknown/…) |
| `!!! HardFault !!!` | сработал HardFault (далее — `NVIC_SystemReset`) |

## 10. Сборка / прошивка

```powershell
& "$env:USERPROFILE\.platformio\penv\Scripts\pio.exe" run -e genericCH32V317WCU6
& "$env:USERPROFILE\.platformio\penv\Scripts\pio.exe" run -e genericCH32V317WCU6 -t upload
```

Итог: сборка без предупреждений, `RAM 9.6% (6264 B)`, `Flash 10.4% (27348 B)`, загрузка — `** Verified OK **`, в логе COM4 нет ошибок.

## 11. Открытые вопросы и рекомендации

1. **Запрос в WCH**: раскладка `R16_UH_SPLIT_DATA`, последовательность SSPLIT/CSPLIT, роль `RB_UH_R_DATA_NO` / `RB_UH_T_DATA_NO` и смысл `RB_UMS_SPLIT_CAN`. При получении ответа этап 2 можно завершить быстро: зонд и примитивы уже готовы, останется заменить «зонд» на рабочие `USBHSH_HubCtrlTransfer` / `HubGetEndpData` / `HubSendEndpData`.
2. **LS-устройства за хабом на USBHS** — закрыто как аппаратно невозможное (нет PRE). Варианты: порт USBFS либо подключение LS напрямую в USBHS.
3. HS-устройства за хабом на USBHS в текущем режиме работают на FS — осознанный компромисс (`DEF_USBHS_HUB_FS_MODE 1`).
4. Опционально: тесты горячего подключения/отключения на обоих портах; «тихий» лог для эксплуатации (`DEF_DEBUG_HUB_SCAN 0`, `DEF_DEBUG_HID_REPORT 0`).


---

## 12. FreeRTOS (ветка `add_freertos`)

Проект переведён на FreeRTOS (вендоренное ядро V10.4.6 + WCH RISC-V порт) вариантом
«A» — всё статично в репозитории, `framework` платформы не используется. Полное описание,
метрики и переключатели: **`FREERTOS_MIGRATION.md`**. Коротко:

* `DEF_FREERTOS_EN 1` — планировщик и две задачи (`usb_host` + bring-up `rtos_test`),
  `0` — прежний bare-metal суперцикл (полный откат одним макросом);
* блокирующие `Delay_Us/Delay_Ms` переведены с SysTick на **TIM4** (SysTick — тик RTOS);
* сборка переведена на дефолтный для платы soft-float ABI `rv32imacxw/ilp32`
  (в проекте нет float, вендоренный порт не правился);
* проверено на железе: тик 1000 тиков = 1999…2000 мс при 500 Гц, `Delay_Ms(10)` = 10 мс,
  перечисление USBFS (FE1.1s + LS-мышь) и USBHS (CH334, HS→FS), непрерывный поток
  HID-отчётов, `heapMin` без утечек, ошибок/HardFault нет;
* размеры: Flash 36 320 B (13.9 %), RAM 19 184 B (29.3 %) при лимитах 256 КБ / 64 КБ;
* известное: задачи «замирают» на время перечисления (~18-20 с, пока идёт
  `vTaskSuspendAll()` вокруг `USBH_MainDeal()`), и печать двух задач без мьютекса
  может изредка перемешиваться (лечится `DEF_RTOS_TEST_TASK 0`).
