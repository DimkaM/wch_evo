# Раскладка пинов проекта `test_usb_1` (CH32V317) + питание ATX

**Проект:** `D:\src\new_evo\test_usb_1` (PlatformIO, env `genericCH32V317WCU6`, сборка с `-DCH32V30x_D8C`)
**Ветка:** `power` (в этой ветке документ и создан)
**МК:** CH32V317WCU6, `SystemCoreClock = 96 МГц` (`SYSCLK_FREQ_96MHz_HSE`, `src/system_ch32v30x.c:24`)
**Дата:** 24.09.2026

Документ — единая точка правды по выводам: что уже занимает код, что свободно и что
зарезервировано под управление ATX-питанием. Смежные документы: `FPGA_CONFIG.md`
(конфигурация ПЛИС), `FREERTOS_MIGRATION.md` (задачи/таймеры), `RESEARCH_CONCLUSION.md` (USB-хост).

---

## 1. Сводка: пины, занятые кодом сейчас

| Пин | Сигнал | Режим GPIO | Где задано | Примечание |
|---|---|---|---|---|
| **PA2** | FPGA `nCONFIG` | выход PP, 50 МГц | `src/fpga.h:43`, `src/fpga.c:61-65` | сброс конфигурации ПЛИС |
| **PA3** | FPGA `nSTATUS` | вход floating | `src/fpga.h:44`, `src/fpga.c:67-70` | готовность ПЛИС к приёму |
| **PA4** | FPGA `CONF_DONE` | вход floating | `src/fpga.h:45`, `src/fpga.c:67-70` | конфигурация принята |
| **PB3** | FPGA `DCLK` (SPI1_SCK) | AF PP, 50 МГц, SPI1 **полный ремап** | `src/fpga.h:46`, `src/fpga.c:57-76` | 6 МГц (96 МГц / 16) |
| **PB5** | FPGA `DATA0` (SPI1_MOSI) | AF PP, 50 МГц | `src/fpga.h:47`, `src/fpga.c:57-76` | RBF передаётся LSB first |
| **PA9** | `USART1_TX` — лог (COM4) | AF PP, 115200 8N1, **только TX** | `src/debug.c:186-192`, `src/main.c:56` | `DEBUG = DEBUG_UART1` |
| **PA11** | USBFS `D−` | периферия USBFS (аппаратное назначение, кодом не переназначается) | `src/USB_Host/*`, `lib/ch32v30x` | к порту подключён хаб FE1.1s + HID |
| **PA12** | USBFS `D+` | периферия USBFS | там же | — |
| **PA13** | `SWDIO` (отладчик wch-link) | SWD, отладочный интерфейс | `platformio.ini` (`upload_protocol = wch-link`) | не занимать/не переназначать |
| **PA14** | `SWCLK` | SWD | там же | не занимать/не переназначать |

### 1.1 Питание ATX (ветка `power`, реализовано)

| Пин | Сигнал | Режим GPIO | Где задано | Примечание |
|---|---|---|---|---|
| **PC0** | `POWER_ON` → база NPN → БП `PS_ON#` | выход PP, 2 МГц (`GPIO_Mode_Out_PP`) | `src/power.h:30-34`, `src/power.c:42-48` | **1 = БП включён** (ключ открыт, `PS_ON#` ≈ 0 В); 0 = выключен |
| **PC1** | `POWER_GOOD` ← БП `PWR_OK` | вход с подтяжкой вниз (`GPIO_Mode_IPD`) | `src/power.h:30-34`, `src/power.c:50-55` | активный высокий: 1 = питания в норме; снятый/отсутствующий провод читается как 0 |

Подробно — §4 (обвязка, полярность, код, порядок запуска).

### 1.2 Кнопка управления (ветка `power`)

| Пин | Сигнал | Режим GPIO | Где задано | Примечание |
|---|---|---|---|---|
| **PC2** | `BTN` — кнопка на GND | вход `GPIO_Mode_IPU` (внешняя подтяжка к VDD + внутренняя как страховка) | `src/button.h:24-25`, `src/button.c:32-43` | активный низкий: 0 = нажата, 1 = отжата; антидребезг 20 мс |

Поведение и параметры — §10.

Периферия **без выводов** (пины не занимает, но важна для контекста):

| Блок | Назначение | Где в коде |
|---|---|---|
| TIM3 | 1 мс: `g_ms_ticks` + интервалы опроса HID/HUB | `src/main.c:63`, `src/USB_Host/app_km.c:49-79, 88` |
| TIM4 | 1 мкс база для `Delay_Us`/`Delay_Ms` | `src/debug.c:39-53` (`DEF_DELAY_TIMER_BASED 1`, `include/debug.h:28`) |
| SysTick | тик FreeRTOS, 500 Гц | `include/FreeRTOSConfig.h:97` |
| SPI1 + DMA1_Channel3 | заливка RBF в ПЛИС | `src/fpga.c:52-103` |
| USBFS | USB-хост | `src/USB_Host/ch32v30x_usbfs_host.c` |

---

## 2. FPGA (Altera, passive serial)

| Сигнал ПЛИС | Пин МК | Режим | Комментарий |
|---|---|---|---|
| `nCONFIG` | PA2 | выход PP | 0 → сброс и начало конфигурации, 1 → отпущен |
| `nSTATUS` | PA3 | вход floating | ПЛИС поднимает в 1, когда готова; 0 во время передачи = ошибка |
| `CONF_DONE` | PA4 | вход floating | ПЛИС поднимает в 1 после успешного приёма |
| `DCLK` | PB3 | SPI1_SCK (полный ремап) | при финализации становится обычным выходом PP и выдаёт 20 импульсов программно |
| `DATA0` | PB5 | SPI1_MOSI (полный ремап) | при финализации = обычный выход PP, удерживается в 1 |
| `GND` | — | — | общий провод |

* Полный ремап: `GPIO_PinRemapConfig( GPIO_Remap_SPI1, ENABLE )` — `src/fpga.c:59`.
* SPI1: master, 8 бит, CPOL=0/CPHA=1, NSS программный, `SPI_FirstBit_LSB`, prescaler 16 → DCLK 6 МГц.
* Детали последовательности, таймауты и параметры — в `FPGA_CONFIG.md`.
* **PB3/PB4/PA15 — JTAG-выводы.** Ремап SPI1 JTAG не выключает (и код, и рабочий проект
  `test_altera` делают только `GPIO_Remap_SPI1`, без `GPIO_Remap_SWJ_*`); на железе такая
  конфигурация проверена. Лежащий в библиотеке `GPIO_Remap_SWJ_Disable` выключает SWD целиком —
  его использовать нельзя, иначе пропадёт отладка (`wch-link`).

---

## 3. Отладочный UART (USART1)

| Пин | Сигнал | Режим |
|---|---|---|
| PA9 | `USART1_TX` | AF PP, 50 МГц |

Инициализация — `src/debug.c:186-192`: включается `RCC_APB2Periph_USART1 | RCC_APB2Periph_GPIOA`,
PA9 переводится в `GPIO_Mode_AF_PP`, `USART_Mode = USART_Mode_Tx` (RX **не** настраивается —
PA10 свободен). Скорость 115200 (`USART_Printf_Init(115200)`, `src/main.c:56`).
SDI-printf не используется (`SDI_PRINT = SDI_PR_CLOSE`, `include/debug.h:45`), поэтому SWD-канал
лога не занят и PA13/PA14 остаются только отладчику.

---

## 4. Питание ATX: `POWER_ON` (PS_ON#) и `POWER_GOOD` (PWR_OK)

Ветка `power`: два вывода, которые связывают МК с ATX-блоком питания. **Реализовано** в
`src/power.h` / `src/power.c`; вызовы добавлены в `src/app_tasks.c` (`POWER_Init`),
`src/fpga.c` (`POWER_On` перед конфигурацией ПЛИС) и `src/main.c` (bare-metal режим).

### 4.1 Раскладка, полярность и обвязка

| Сигнал | Пин МК | Режим GPIO (SPL) | Уровни | Обвязка на плате |
|---|---|---|---|---|
| `POWER_ON` (→ БП `PS_ON#`, пин 16, зелёный) | **PC0** | `GPIO_Mode_Out_PP`, `GPIO_Speed_2MHz` | **1 = БП включён**; 0 = выключен | ключ на NPN: PC0 →[1 кОм]→ база, эмиттер → GND, коллектор → `PS_ON#` (открытый коллектор) |
| `POWER_GOOD` (← БП `PWR_OK`, пин 8, серый) | **PC1** | `GPIO_Mode_IPD` (внутренняя подтяжка вниз) | **1 = питания в норме**; 0 = нет | делитель 12 кОм (от `PWR_OK`) / 20 кОм (на GND) + 100 нФ на GND; при снятом проводе уровень задаёт внутренняя подтяжка (0) |

Схема ключа `POWER_ON`:

```
PC0 ──[1 кОм]── база NPN ─┬──────────┐
                          │          │
                      эмиттер    коллектор ── PS_ON# (внутри БП подтянут к +5 V SB)
                          │
                         GND
```

Почему именно так:

* `PS_ON#` в ATX — **активный низкий** и подтянут к `+5 V SB` внутри БП, поэтому линию тянет вниз
  транзистор (открытый коллектор), а сам вывод МК с 5 В не соприкасается — ему достаточно обычного
  push-pull. Ток базы ≈ (3.3 − 0.7) В / 1 кОм ≈ **2.6 мА**, коллектор пропускает ток `PS_ON#`
  (десятые доли мА … 1.6 мА) с большим запасом по насыщению.
* Полярность инвертирует железо: **PC0 = 1 ⇒ БП включён** (`PS_ON#` ≈ 0 В), PC0 = 0 ⇒ ключ закрыт,
  `PS_ON#` подтянут к `+5 V SB` ⇒ БП выключен. В коде это скрыто макросами
  `PWR_PIN_ON_ASSERT()` / `PWR_PIN_ON_RELEASE()` (`src/power.h:33-34`).
* Рекомендуемый резистор **10 кОм база–эмиттер**: пока МК в сбросе/прошивается, PC0 — вход (Hi-Z),
  и без этой подтяжки состояние ключа неопределённо. С резистором при сбросе МК ключ закрыт,
  то есть БП гарантированно выключен.
* `PWR_OK` — **активный высокий**, `+5 В` TTL, появляется через **100–500 мс** после включения
  (спецификация ATX) и пропадает не позже чем за ~1 мс до выхода питаний из допуска; ниже ~2.0 В —
  «power bad». Выводы порта C — это ADC-каналы (`ADC_Channel_10/11`), то есть почти наверняка
  **не 5 В-толерантные** (`FT`), поэтому делитель обязателен: 5.0 В → 3.13 В, 4.75 В → 2.97 В,
  5.25 В → 3.28 В (ниже VDD и с запасом выше VIH ≈ 2.31 В), ток из линии `PWR_OK` ≈ 0.16 мА
  (ATX допускает ~1.6 мА).
* Вход именно `IPD` (внутренняя подтяжка вниз), а не floating: если провод `PWR_OK` снят или
  БП отсутствует, вывод **обязан** читаться как 0. С `IN_FLOATING` снятый провод давал случайный
  уровень — проверено на железе (тест со снятым `PWR_OK` показывал `POWER_GOOD=1` за 46 мс).
  С делителем 12к/20к и внутренними 30–50 кОм уровень ≈ 2.6–2.9 В — выше VIH (≈2.31 В), запас
  ~0.3–0.6 В; если нужен больший запас, уменьшить R1 до 8.2 кОм (уровень ≈ 3.1 В).
* Фактический монтаж (проверено): делитель 12к/20к стоит **на стороне БП**, поэтому при
  отключении провода со стороны МК вывод PC1 остаётся без внешней подтяжки. Рабочие варианты:
  нижнее плечо (20 кОм) перенести на сторону МК, либо (как сделано) полагаться на внутреннюю
  `IPD` — тогда снятый провод читается как 0 без доработки платы. Разница уровней: с делителем
  только на стороне БП и `IPD` получается ≈2.6–2.9 В (всё ещё 1), без внутренней подтяжки было бы
  3.13 В.
* `GPIO_Mode_Out_PP` / `GPIO_Mode_IPD` — `lib/ch32v30x/inc/ch32v30x_gpio.h:32-38`.
* Перед `POWER_Init()` выводы в состоянии сброса (вход, Hi-Z) ⇒ ключ закрыт, БП выключен; включается
  он только по `POWER_On()` после того, как поднимется USB.
* PC0/PC1 выбраны потому, что **весь порт GPIOC в прошивке не используется** (см. §5), рядом
  с ними нет ничего занятого, и оба вывода — обычные GPIO (никакие альтернативные функции на них
  в проекте не включены).
  Свободные выводы в прошивке ≠ свободные на плате — **сверить с схемой/разъёмом конкретной платы**.

### 4.2 Реализация (`src/power.h`, `src/power.c`)

| Элемент | Где | Смысл |
|---|---|---|
| `DEF_POWER_EN` | `src/power.h` | 1 — управление ATX включено; 0 — откат (PSU не трогается, всё как до ветки `power`) |
| `DEF_POWER_GOOD_TIMEOUT_MS` | `src/power.h` | 500 мс — таймаут ожидания `PWR_OK` |
| `DEF_POWER_SETTLE_MS` | `src/power.h` | 300 мс — запас после `PWR_OK`: местным стабилизаторам платы ПЛИС и её POR нужно время (с 50 мс ловили `nSTATUS timeout`) |
| `DEF_POWER_OFF_CONFIRM_MS` | `src/power.h` | 2000 мс — ожидание падения `PWR_OK` перед повторным включением БП (чистый power-cycle) |
| `DEF_POWER_RETRY` | `src/power.h` | 1 — одна повторная попытка (power cycle) после таймаута `PWR_OK` |
| `PWR_PIN_ON_ASSERT()` / `PWR_PIN_ON_RELEASE()` | `src/power.h` | PC0 = 1 (ключ открыт, БП включён) / PC0 = 0 (БП выключен) |
| `POWER_Init()` | `src/power.c` | часы GPIOC; PC0 сначала ODR = 0, затем `Out_PP` 2 МГц; PC1 = `IPD` |
| `POWER_IsGood()` | `src/power.c` | текущее состояние `PWR_OK` |
| `POWER_WaitGood()` | `src/power.c` | ожидание `PWR_OK` с таймаутом по `g_ms_ticks` |
| `POWER_WaitClear()` | `src/power.c` | ожидание падения `PWR_OK` (рельсы разрядились) |
| `POWER_On()` | `src/power.c` | при необходимости чистый power-cycle → включить БП → ждать `PWR_OK` → пауза settle; при неудаче один повтор |
| `POWER_Off()` | `src/power.c` | отпустить `PS_ON#` — БП выключается |
| `AppPower_Init/Startup/Step/Task` | `src/app_power.c` | последовательность «питание + ПЛИС + кнопка» (единственный владелец `POWER_*` и `FPGA_Config()`), повтор заливки при неудаче |

Задержки внутри модуля идут через `PWR_DelayMs` (`vTaskDelay` в RTOS / `Delay_Ms` в bare-metal,
приём из `src/fpga.c:30-36`), таймауты считаются по счётчику `g_ms_ticks` (1 мс, TIM3) — то есть
в реальных миллисекундах, как в `FPGA_Config()`.
Лог-строки модуля (COM4):

```
PWR: init, PS_ON# released (PSU off)
PWR: POWER_ON asserted (attempt 1)
PWR: POWER_GOOD=1 (N ms)              <- N <= 500 при исправном БП
PWR: POWER_GOOD timeout (N ms)        <- БП не ответил
PWR: retry (PSU power cycle)          <- идёт одна повторная попытка
PWR: PS_ON# released (PSU off)        <- из POWER_Off()
```

### 4.3 Порядок запуска (реализовано)

Стартовая последовательность (RTOS-режим) — питание включается **до** конфигурации ПЛИС, потому
что ПЛИС и её обвязка питаются от БП:

```
AppTasks_Start():   POWER_Init()      -> PC0 = 0, ключ закрыт, PS_ON# отпущен, БП выключен
fpga_cfg:           FPGA_DelayMs(3000) -> USB уже поднят (перечисление хаба/HID)
fpga_cfg:           POWER_On()        -> PC0 = 1, ждём PWR_OK (<= 500 мс) + 50 мс settle
fpga_cfg:           FPGA_Config()     -> nCONFIG, RBF 98023 Б, CONF_DONE
fpga_cfg:           vTaskDelete(NULL) -> задача завершается, повторных заливок нет
```

Точки вызова в коде:

* `src/app_tasks.c:127-131` — `POWER_Init()` в `AppTasks_Start()` (БП гарантированно выключен до
  включения планировщика);
* `src/fpga.c:264-272` — `POWER_On()` в `FPGA_ConfigTask()`: при неудаче печатается
  `FPGA: no POWER_GOOD, configuration skipped`, задача удаляется, ПЛИС **не** конфигурируется;
* `src/main.c:90-94` — `POWER_Init()` в bare-metal режиме (после `__enable_irq()`);
* `src/main.c:118-125` — `POWER_On()` перед `FPGA_Config()` в суперцикле (bare-metal).

Прочее:

* `DEF_POWER_EN 0` возвращает прежнее поведение одной строкой (PSU не трогается).
* Питание МК: логика «включить/выключить БП» корректна, только если МК запитано от `+5 V SB`
  (дежурка). Если МК питается от основных рельсов, выключение БП снимет питание и с МК
  (допустимо как «мягкое выключение», но повторное включение логикой станет невозможно).
* `POWER_Off()` — основа будущей функции выключения (удержание `PS_ON#` в 0 всё время работы
  обязательно: отпустив линию, БП выключится сам).
* Сброс МК при работающем БП (перепрошивка, HardFault → `NVIC_SystemReset()` в
  `src/ch32v30x_it.c:38-45`) закрывает ключ и **выключает БП** (если стоит резистор 10 кОм
  база–эмиттер) — это ожидаемое поведение текущей схемы.

---

## 5. Карта портов: занято / свободно

**Порт A (GPIOA)** — часы включены в `src/debug.c:187` и `src/fpga.c:53-54`.

| Пин | Состояние |
|---|---|
| PA0, PA1 | свободны |
| PA2 | FPGA `nCONFIG` |
| PA3 | FPGA `nSTATUS` |
| PA4 | FPGA `CONF_DONE` |
| PA5, PA6, PA7 | свободны (SCK/MISO/MOSI SPI1 по умолчанию: не используются, включён **полный ремап** SPI1) |
| PA8 | свободен |
| PA9 | `USART1_TX` (лог 115200) |
| PA10 | свободен (`USART1_RX` не настроен) |
| PA11 | USBFS `D−` |
| PA12 | USBFS `D+` |
| PA13, PA14 | `SWDIO`/`SWCLK` — отладчик, не занимать |
| PA15 | свободен, но это `JTDI` (JTAG) — см. §6 |

**Порт B (GPIOB)** — часы включены в `src/fpga.c:53-54`.

| Пин | Состояние |
|---|---|
| PB0, PB1 | свободны |
| PB2 | свободен (`BOOT1`; состояние при сбросе задаётся платой) |
| PB3 | FPGA `DCLK` (SPI1_SCK, полный ремап) |
| PB4 | свободен, но `JNTRST`; `SPI1_MISO` в ремапе не подключается (передача односторонняя) |
| PB5 | FPGA `DATA0` (SPI1_MOSI) |
| PB6…PB15 | свободны (альтернативы по ремапу: USART1/CAN1/I2C1 SCK-SDA и др. — не используются) |

**Порт C (GPIOC)** — до ветки `power` код порт **не использовал вообще**; теперь здесь живут
`POWER_ON` / `POWER_GOOD` (§4), остальные выводы свободны.

| Пин | Состояние |
|---|---|
| PC0 | **`POWER_ON`** → NPN-ключ → БП `PS_ON#` (реализовано, `src/power.h:31`) |
| PC1 | **`POWER_GOOD`** ← БП `PWR_OK` (реализовано, `src/power.h:32`; внешний делитель 12к/20к) |
| PC2 | **`BTN`** — кнопка управления (реализовано, `src/button.h:25`, §10) |
| PC3…PC15 | свободны |

**Порты D / E** — кодом не используются (наличие выводов зависит от корпуса — проверять по
даташиту/схеме).

Важно: `GPIO_IPD_Unused()` (подтянула бы все неиспользуемые выводы к земле, реализация —
`src/ch32v30x/ch32v30x_gpio.c:577`) в проекте **закомментирована**
(`src/system_ch32v30x.c:236`). Следствия:

1. Нерасписанные выводы остаются в состоянии сброса (вход floating) — новые **входы** нужно
   конфигурировать явно (`IPD`/`IPU`/внешний резистор); `POWER_GOOD` уже настроен как
   `IN_FLOATING` + внешний делитель 12к/20к (§4.1).
2. Если её включить, она переведёт нерасписанные выводы (в том числе PC0/PC1) в вход с подтяжкой
   вниз: управление БП перестанет работать (PC0 больше не удерживает ключ открытым), а на PC1
   внутренние 30–50 кОм встанут параллельно нижнему плечу делителя и занизят уровень `PWR_OK`.
   Функция в проекте выключена — вместе с веткой `power` её включать нельзя.

---

## 6. Ограничения и особые случаи

| Пин/группа | Ограничение |
|---|---|
| PA13 / PA14 | SWD-отладка (`wch-link`). Переназначать/использовать под GPIO нельзя, иначе теряется загрузка и отладка |
| PB3, PB4, PA15 | JTAG-выводы по умолчанию. Ремап SPI1 (`GPIO_Remap_SPI1`) JTAG **не** отключает — и в этом проекте, и в рабочем `test_altera` PB3 как `DCLK` проверен на железе. Полное отключение SWJ (`GPIO_Remap_SWJ_Disable`) не применять: пропадёт отладка |
| PA11 / PA12 | жёстко за периферией USBFS (ремап не поддерживается) |
| SPI1 | включён **полный ремап** (`src/fpga.c:59`): PA5/PA6/PA7 свободны, `SCK = PB3`, `MOSI = PB5`. MISO не используется (передача односторонняя, DMA TX) |
| PC1 (`POWER_GOOD`) | выводы порта C — ADC-каналы, то есть **не 5 В-толерантные** (`FT`): обязателен внешний делитель `+5 В → 3.3 В` (12к/20к + 100 нФ); вход `IPD`, чтобы снятый провод читался как 0 (см. §4.1) |
| PC0 (`POWER_ON`) | выход `Out_PP` (`GPIO_Mode_Out_PP`), нагрузка — база NPN через 1 кОм (ток базы ≈ 2.6 мА, насыщение обеспечено); напрямую в `PS_ON#` подключать нельзя |
| SysTick | занят FreeRTOS (тик 500 Гц), задержки живут на TIM4 — при добавлении нового кода использовать `Delay_Us/Delay_Ms` и `vTaskDelay`, но не перенастраивать SysTick |
| PC2 (`BTN`) | кнопка на GND, подтяжка к VDD внешняя + внутренняя `IPU`; антидребезг обязателен (`src/button.c`), «сырое» чтение пина в логике не использовать |

---

## 7. Как добавить или переопределить пин

1. Правку делать **в одном месте** и здесь же обновить этот документ:
   * пины ПЛИС — `src/fpga.h:41-47` (порт/пин/режим), инициализация `src/fpga.c:52-76`;
   * пины питания — макросы `PWR_PORT_CTRL` / `PWR_PIN_ON` / `PWR_PIN_GOOD` в `src/power.h:29-33` (§4);
   * UART лога — `include/debug.h` (`DEBUG`) + `src/debug.c:186-210`.
2. Проверить **часы порта**: `RCC_APB2PeriphClockCmd( RCC_APB2Periph_GPIOx, ENABLE )`
   (GPIOC тактируется в `POWER_Init()`, `src/power.c:40`).
3. Проверить конфликт по таблицам §1 и §5: пин не должен быть занят периферией, JTAG/SWD
   или USBFS.
4. Настроить вход явно (floating/IPU/IPD) — авто-подтяжки нет (§5); пример — `POWER_GOOD`
   с внешним делителем и `GPIO_Mode_IN_FLOATING`.
5. Собрать и прошить:
   ```powershell
   & "$env:USERPROFILE\.platformio\penv\Scripts\pio.exe" run -e genericCH32V317WCU6
   & "$env:USERPROFILE\.platformio\penv\Scripts\pio.exe" run -e genericCH32V317WCU6 -t upload
   ```

---

## 8. Чек-лист железа (ветка `power`)

* [ ] PC0/PC1 действительно разведены на цепь управления БП (в прошивке порт C свободен, но на PCB
      это не гарантировано).
* [ ] NPN-ключ: pinout проверен (база — через 1 кОм от PC0, эмиттер — на общий `GND` с БП,
      коллектор — на `PS_ON#`), рекомендуемый резистор 10 кОм база–эмиттер установлен.
* [ ] `PWR_OK` подключён к PC1 через делитель 12 кОм / 20 кОм + 100 нФ (фактически делитель
      припаян **на стороне БП**, проверено).
* [ ] Мультиметр на PC1: при подключённом `PWR_OK` ≈ 2.6–3.1 В, при **снятом** проводе ≈ 0 В
      (проверка, что подтяжка/делитель реально есть со стороны МК и аварийная ветка сработает).
* [ ] Измерено мультиметром: PC0 = 3.3 В (БП включён) / 0 В (выключен); `PS_ON#` ≈ 5 В выключено и
      < 0.8 В включено; при сбросе МК (reset/перепрошивка) БП остаётся выключенным.
* [ ] МК питается от `+5 V SB` (иначе выключение БП = потеря управления).
* [ ] В логе COM4: `PWR: POWER_GOOD=1 (N ms)` приходит с N ≤ 500; при снятом `PWR_OK` —
      `PWR: POWER_GOOD timeout` и **нет** строки `FPGA: CONF_DONE=1` (негативный тест).
* [ ] Кнопка: PC2 замыкается на GND, подтяжка к VDD есть; в логе `BTN: init (idle=1)` и
      `BTN: armed`. Проверить: короткое нажатие → `BTN: short press … -> FPGA reconfiguration`
      (ровно одно срабатывание на нажатие), удержание > 3 с → `BTN: long press … -> PSU off`
      **до** отпускания, нажатие при выключенном БП → `BTN: pressed, PSU off -> power on` + заливка.

---

## 9. Быстрая справка «пин → файл»

| Что | Файл:строки |
|---|---|
| Пины и параметры ПЛИС | `src/fpga.h:41-47`, `src/fpga.c:52-76`, `src/fpga.c:136-155` |
| Последовательность конфигурации ПЛИС | `src/fpga.c:165-241` (описание — `FPGA_CONFIG.md`) |
| UART лога (PA9) | `src/debug.c:186-192`, `src/main.c:56` |
| TIM3 (1 мс, `g_ms_ticks`) | `src/USB_Host/app_km.c:49-79, 88`, `src/main.c:63` |
| TIM4 (задержки) | `src/debug.c:39-53`, `include/debug.h:28` |
| Тик FreeRTOS (SysTick, 500 Гц) | `include/FreeRTOSConfig.h:97` |
| USBFS-хост (PA11/PA12) | `src/USB_Host/ch32v30x_usbfs_host.c`, `src/USB_Host/usb_host_config.h` |
| Отладчик/загрузка (SWD) | `platformio.ini` |
| Питание ATX (PC0/PC1) | `src/power.h` (макросы), `src/power.c` (реализация) |
| Точки вызова питания | `src/app_tasks.c:127-131`, `src/fpga.c:264-272`, `src/main.c:90-94, 118-125` |
| Кнопка (PC2), антидребезг | `src/button.h`, `src/button.c` |
| Последовательность «питание + кнопка + ПЛИС» | `src/app_power.h`, `src/app_power.c` |
| Политика USB (стоп/рестарт по БП) | `src/app_usb.h`, `src/app_usb.c`, `src/USB_Host/usb_host_config.h` (`DEF_USB_*`) |
| Точки входа USB-стека | `src/USB_Host/app_km.h` (`USBH_StackInit/Down`), `src/USB_Host/app_km.c` (в конце файла) |

---

## 10. Кнопка управления и USB-политика (реализовано)

### 10.1 Кнопка (PC2)

| Параметр | Где | Значение / смысл |
|---|---|---|
| Пин | `src/button.h:24-25` | PC2, кнопка замыкает на GND, внешняя подтяжка к VDD → **0 = нажата** |
| Режим входа | `src/button.c:36-40` | `GPIO_Mode_IPU` (внутренняя подтяжка как страховка) |
| Антидребезг | `DEF_BTN_DEBOUNCE_MS` = 20 мс | новый уровень принимается только после 20 мс неизменности; фильтр по времени `g_ms_ticks`, поэтому не зависит от темпа опроса |
| Период опроса | `DEF_BTN_POLL_MS` = 2 мс | из задачи `AppPowerTask` (RTOS) или из суперцикла (bare-metal) |
| Длинное нажатие | `DEF_BTN_LONG_MS` = 3000 мс | порог выключения БП |
| Логика | `src/app_power.c` | «взвод» по первому подтверждённому уровню: если кнопка зажата с момента старта — ждём отпускания, иначе `BTN: armed` появляется сразу; подавление повторных действий до отпускания; блокировка `DEF_PWR_OFF_LOCKOUT_MS` = 1000 мс после выключения (ждём падения `PWR_OK`) |

Поведение:

| Событие | Условие | Действие |
|---|---|---|
| Нажатие | `PWR_OK == 0` | `POWER_On()` → `FPGA_Config()` — сразу, не дожидаясь отпускания |
| Отпускание, удержание < 3 с | `PWR_OK == 1` | `FPGA_Config()` — перезаливка ПЛИС |
| Удержание ≥ 3 с | `PWR_OK == 1` | `POWER_Off()` — сразу, не дожидаясь отпускания |

Лог: `BTN: init (idle=1)`, `BTN: armed`, `BTN: pressed, PSU off -> power on`,
`BTN: pressed, PSU on (hold >= 3000 ms = long press)`,
`BTN: short press (N ms) -> FPGA reconfiguration`, `BTN: long press (N ms) -> PSU off`.

### 10.2 USB-политика (дефайны в `src/USB_Host/usb_host_config.h:69-86`)

| Макрос | Значение | Смысл |
|---|---|---|
| `DEF_USB_OFF_WHEN_PSU_OFF` | 1 | пока `PWR_OK == 0`, стек остановлен (`USBH_StackDown()`) и `USBH_MainDeal()` не вызывается |
| `DEF_USB_RESTART_ON_POWER_ON` | 1 | каждый фронт `PWR_OK` 0→1 — перезапуск стека (полное перечисление) |
| `DEF_USB_RESTART_ON_FPGA_CONFIG` | 0 | 1 — перезапускать стек ещё и после каждой заливки ПЛИС (если заливка мешает USB-части) |
| `DEF_USB_RESTART_DELAY_MS` | 0 | пауза между запросом и выполнением перезапуска |

Реализация — `src/app_usb.c` (`AppUsb_Init/AppUsb_Step/AppUsb_RequestRestart`); состояние контроллера
и перечисления — `USBH_StackInit()` / `USBH_StackDown()` (`src/USB_Host/app_km.c`, конец файла;
`USBH_StackInit()` на работающем стеке = перезапуск, драйвер сам сбрасывает SIE).
Лог: `USB: stack stopped (PSU off)`, `USB: stack started (PSU on)`, `USB: restart on power on`,
`USB: restarted on request`, `USB: stack initialised (PSU on|off)`.

### 10.3 Диагностика монтажа (`DEF_PINS_DEBUG`)

В `src/app_power.h` есть переключатель `DEF_PINS_DEBUG` (для bring-up включён): раз в секунду
в лог печатается строка с «сырыми» уровнями выводов питания и кнопки:

```
PINS: CFGLR=44444442 OUTDR=00000004 INDR=00000007 | PC0 drv=0 pin=1 | PC1(ok)=0 | PC2(btn)=1 | PSU=0 BTN=0
```

* `CFGLR`/`OUTDR`/`INDR` — сырые регистры порта C: по `CFGLR` видно режим вывода
  (младшая тетрада — PC0: `2` = выход PP 2 МГц, `4` = вход floating, `8` = вход с подтяжкой),
  по `OUTDR` — что МК «выдаёт», по `INDR` — что реально на выводах;
* `PC0 drv` — что МК **выдаёт** на PC0 (1 = «БП включён»), `PC0 pin` — что **реально читается**
  с вывода; расхождение означает перегрузку/закоротку вывода (проблема монтажа);
* `PC1(ok)` — уровень на входе `POWER_GOOD`, `PC2(btn)` — уровень на кнопке, `PSU` — то же через
  `POWER_IsGood()`, `BTN` — отфильтрованное (антидребезг) состояние кнопки.

Сверка этих чисел с мультиметром на выводах МК и на ключе быстро выявляет перепутанный или
отвалившийся провод. Для «тихого» лога поставить `DEF_PINS_DEBUG 0`.

---

**История:**

* 24.09.2026 — создан (ветка `power`): зафиксированы занятые выводы (FPGA PA2/PA3/PA4/PB3/PB5,
  UART PA9, USBFS PA11/PA12, SWD PA13/PA14) и добавлены два пина управления ATX:
  `POWER_ON` → PC0, `POWER_GOOD` → PC1.
* 24.09.2026 — **реализовано**: `src/power.h` / `src/power.c` (`POWER_Init/On/Off/IsGood`),
  вызовы в `AppTasks_Start()` и `FPGA_ConfigTask()` + bare-metal ветка `main()`. Полярность
  уточнена по железу: PC0 управляет NPN-ключом (**1 = БП включён**), `PWR_OK` заведён на PC1 через
  делитель 12к/20к (вход `IN_FLOATING`). Заливка ПЛИС идёт только после `PWR_OK`. Сборка —
  `[SUCCESS]`, RAM 27.1% (17 768 Б), Flash 50.8% (133 208 Б).
* 24.09.2026 — **проверено на железе**: нормальный прогон `PWR: POWER_GOOD=1 (334 ms)` →
  `FPGA: CONF_DONE=1`, `SystemClk:96000000` (96 МГц подтверждён). Негативный прогон (провод
  `PWR_OK` снят) с `IN_FLOATING` **не** сработал: «висящий» вход читался как 1 (`POWER_GOOD=1`
  за 46 мс) и ПЛИС всё равно конфигурировалась → PC1 переведён на `IPD` (внутренняя подтяжка вниз),
  чтобы отсутствие `PWR_OK` гарантированно читалось как 0.
* 24.09.2026 — **оба пути проверены на железе** (бинарник с `IPD`): с подключённым `PWR_OK` —
  `PWR: POWER_GOOD=1 (346 ms)` → `FPGA: CONF_DONE=1` (уровень ≈2.6 В с делителем на стороне БП
  уверенно читается как 1); со снятым проводом — `PWR: POWER_GOOD timeout (501 ms)` →
  `PWR: retry (PSU power cycle)` → `timeout (501 ms)` → `FPGA: no POWER_GOOD, configuration
  skipped`, строк `FPGA: nSTATUS OK`/`CONF_DONE=1` в логе нет.
* 24.09.2026 — **диагностика железа и найденный дефект прошивки**: после рефакторинга проявилось
  «`PC0 drv=0 pin=1`, `CFGLR=44444444`» — порт C оставался в состоянии сброса, БП не включался,
  ПЛИС отвечала `nSTATUS=0`. Причина: в `src/app_tasks.c` вызов `AppPower_Init()` стоял под
  `#if DEF_POWER_EN`, а `power.h` из файла был убран при рефакторинге → макрос стал неопределён
  (0) и инициализация ATX/кнопки вырезалась компилятором. Исправлено: вызов без guard'а (проверки
  внутри `AppPower_Init()`), `src/app_power.h` теперь включает `power.h` и `button.h`. Также
  «взвод» кнопки делается по первому подтверждённому уровню (без предварительного нажатия) и
  добавлена диагностика `PINS:` с сырыми `CFGLR/OUTDR/INDR`. Проверено: `PWR: POWER_GOOD=1
  (343 ms)`, `FPGA: CONF_DONE=1`, `USB: stack stopped (PSU off)` на старте и `USB: stack started
  (PSU on)` после включения БП.
* 24.09.2026 — **кнопка проверена на железе** (все сценарии, прошивка с `DEF_PINS_DEBUG 0`):
  удержание 5 с → `BTN: long press (3000 ms) -> PSU off` + `PWR: PS_ON# released` +
  `USB: stack stopped (PSU off)` (БП выключился до отпускания); нажатие при выключенном БП →
  `BTN: pressed, PSU off -> power on` → `POWER_GOOD=1 (335 ms)` → `USB: stack started (PSU on)` →
  `CONF_DONE=1` (не дожидаясь отжатия); 5 коротких нажатий → ровно 5 событий
  `BTN: short press (146…172 ms) -> FPGA reconfiguration`, каждый раз `nSTATUS OK` →
  `CONF_DONE=1` за 131 мс (антидребезг 20 мс работает, повторные заливки без сбоев).



