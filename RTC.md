# RTC: эмуляция регистров Dallas DS12887 на CH32V317

**Проект:** `D:\src\new_evo\test_usb_1`, ветка `rtc_ena`
**Модуль:** `src/rtc.c` + `src/rtc.h`
**Эталон семантики:** `D:\src\pentevo\avr\baseconf\trunk\src\rtc.{c,h}` (эмуляция «Gluk clock» для ZX Evo)

---

## 1. Назначение

Микросхема DS12887 (часы IBM PC/AT с батарейным питанием, 50 байт NVRAM) **эмулируется**
на внутренних ресурсах CH32V317:

* **время** — счётчик секунд RTC (такт 32.768 кГц от LSE, кварц припаян; при отказе LSE —
  внутренний LSI с более низкой точностью);
* **NVRAM и настройки** — регистры резервного питания **BKP** (VBAT подан);
* **alarm-регистры не эмулируются** (как и в эталоне);
* регистры `A/B/C/D` реализованы «в меру возможного» (ниже).

Доступ к «регистрам» — двумя функциями:

```c
uint8_t rtc_read ( uint8_t addr );                 /* прочитать регистр (0x00..0x3F) */
void    rtc_write( uint8_t addr, uint8_t data );   /* записать регистр              */
void    rtc_init ( void );                         /* один раз при старте           */
```

Вызов `rtc_init()` — в `main()` (после `USART_Printf_Init()`, до запуска задач), при
`DEF_RTC_INIT_EN = 0` инициализация не выполняется, но доступ к регистрам остаётся.

## 2. Карта регистров DS12887

| Адрес | Регистр | Чтение | Запись |
|---|---|---|---|
| `0x00` | секунды | 00–59, в BCD (или двоично при `B.DM=1`) | только при `B.SET=1` |
| `0x01` | секунды alarm | **не эмулируется** → `0x00` | игнорируется |
| `0x02` | минуты | 00–59 | только при `B.SET=1` |
| `0x03` | минуты alarm | **не эмулируется** → `0x00` | игнорируется |
| `0x04` | часы | 00–23 (или 1–12 + бит `0x80`=PM при `B.24/12=0`) | только при `B.SET=1` |
| `0x05` | часы alarm | **не эмулируется** → `0x00` | игнорируется |
| `0x06` | день недели | 1–7 (1 = понедельник), **вычисляется из даты** | только при `B.SET=1` (на чтение не влияет) |
| `0x07` | день месяца | 01–31 (BCD) | только при `B.SET=1` |
| `0x08` | месяц | 01–12 (BCD) | только при `B.SET=1` |
| `0x09` | год | 00–99 (BCD), век принят за 20xx | только при `B.SET=1` |
| `0x0A` | A | `UIP` (бит 7) + `DV2..0 = 010` (0x20) + `RS3..0` | только `RS3..0` (мл. тетрада) |
| `0x0B` | B | `SET`(0x80), `PIE`(0x40), `AIE`(0x20), `UIE`(0x10), `SQWE`(0x08), `DM`(0x04), `24/12`(0x02), `DSE`(0x01) | сохраняется целиком, **работают** `SET`, `DM`, `24/12` |
| `0x0C` | C | только чтение: `UF`(0x10) — встаёт раз в секунду, сбрасывается при чтении; `PF/AF/IRQF = 0` | игнорируется |
| `0x0D` | D | `VRT`(0x80) = время валидно, иначе `0x00` | запись `0x80` → валидно, `0x00` → инвалидно |
| `0x0E…0x3F` | NVRAM (50 байт) | значение из BKP | запись в BKP, сохраняется при потере питания |
| другие | — | `0x00` | игнорируется |

Особенности, совпадающие с реальной микросхемой:

* **`SET=1` останавливает часы**: текущее время снимается в «теневые» регистры, после чего
  `0x00…0x09` можно писать; `SET=0` загружает записанное время в счётчик и запускает его.
  Так время устанавливается **через регистры** (см. §5).
* `UIP` эмулируется по счётчику предделителя: бит встаёт в последние `DEF_RTC_UIP_TICKS`
  тиков секунды (по умолчанию 700 из 32768, ≈21 мс).
* чтение времени не «рвётся» на границе секунды: счётчик читается дважды до совпадения.
* значения вне диапазона при загрузке часов «подрезаются» (сек/мин → 0, месяц → 1, день → 1),
  но байты регистров хранятся ровно так, как записаны.

## 3. Карта регистров BKP (42 регистра × 16 бит)

| BKP | Байты DS12887 | Назначение |
|---|---|---|
| `BKP_DR1` | `0x0E`, `0x0F` | NVRAM (чётный адрес → младший байт) |
| `BKP_DR2` | `0x10`, `0x11` | NVRAM |
| … | … | … |
| `BKP_DR25` | `0x3E`, `0x3F` | NVRAM (итого 25 регистров = 50 байт) |
| `BKP_DR26` | — | **настройки**: мл. байт = регистр `A` (RS3..0), ст. байт = регистр `B` |
| `BKP_DR27` | — | **статус**: мл. байт = метка `0x5A`, ст. байт: бит 7 = `VRT`, биты 3..0 = версия формата |
| `BKP_DR28` | — | «теневые» регистры `0x00`, `0x01` (время при `SET=1`) |
| `BKP_DR29` | — | `0x02`, `0x03` |
| `BKP_DR30` | — | `0x04`, `0x05` |
| `BKP_DR31` | — | `0x06`, `0x07` |
| `BKP_DR32` | — | `0x08`, `0x09` |
| `BKP_DR33…DR42` | — | свободны (резерв) |

Благодаря этому после сброса/перезапуска согласованно восстанавливаются: время (счётчик RTC
в VBAT-домене), настройки `A/B` (`DM`, `24/12`, `RS`), признак валидности `VRT` и «замороженное»
время при активном `SET`. Флаги регистра `C` — программные (живут в RAM, как и на микросхеме
сбрасываются при перезапуске).

## 4. Инициализация (`rtc_init()`)

1. Тактирование `PWR`/`BKP`, `PWR_BackupAccessCmd(ENABLE)` (нужно для доступа к BKP сразу после сброса).
2. Проверяется бит `RTCEN` в `RCC->BDCTLR` **до** включения RTC — он говорит, шёл ли счётчик до этого сброса.
3. Такт: `LSE` (`RCC_LSEConfig(RCC_LSE_ON)`, ожидание `LSERDY` до `DEF_RTC_LSE_WAIT_MS` = 1000 мс)
   → при успехе предделитель `32767`; иначе включается `LSI`, предделитель `39999`, в лог
   печатается предупреждение о низкой точности.
4. `RCC_RTCCLKCmd(ENABLE)`, ожидание синхронизации, установка предделителя.
5. Из BKP читаются настройки (`DR26`) и статус (`DR27`): если метки `0x5A` нет (первый запуск
   или сброс домена резервного питания), берутся значения по умолчанию: `A = 0x00`,
   `B = 0x02` (BCD + 24 часа), `VRT = 0`. Из `DR28…DR32` восстанавливаются теневые регистры.
6. **Время устанавливается**, если выполнено любое из:
   * нет метки в BKP (`magic = 0`) — домен бэкапа был сброшен;
   * `RTCEN = 0` — RTC не был активирован;
   * `RTC_GetCounter() == 0` — счётчик сброшен;
   * `VRT = 0` — время помечено невалидным (в т.ч. программно через запись в регистр `D`).
7. Установка идёт **через регистровый интерфейс** (как это делало бы ПО хоста):
   `SET=1` → запись `0x09, 0x08, 0x07, 0x06, 0x04, 0x02, 0x00` → `D = 0x80` → `SET=0`.
   Значение по умолчанию: **26.09.2026 08:56:37** (`DEF_RTC_INIT_*`, локальное время без
   часового пояса; день недели вычисляется — суббота, `0x06`).

Лог при старте (пример):

```
RTC: clock=LSE, magic=0, enabled=0, cnt=0, VRT=0
RTC: SET=1, clock stopped
RTC: time set to 26.09.2026 08:56:37 (dow=6)
RTC: SET=0, clock started
RTC: VRT=1
RTC regs: 37 00 56 00 08 00 06 26 09 26 | A=20 B=02 C=00 D=80
```

Дамп — это 10 байт `0x00…0x09` (BCD) и регистры `A/B/C/D`; он же проверяет путь чтения.
Повторный запуск без потери VBAT печатает `magic=1, enabled=1, cnt=…, VRT=1` и **время не
переустанавливает**.

## 5. Как установить время или инвалидировать его из кода

```c
/* установка времени (аналог того, что делает ПО хоста) */
rtc_write( DS_REG_B, (uint8_t)( rtc_read( DS_REG_B ) | DS_B_SET ) );  /* остановить часы */
rtc_write( DS_REG_YEAR, 0x26 );      /* 2026, BCD */
rtc_write( DS_REG_MONTH, 0x09 );
rtc_write( DS_REG_DAY_MONTH, 0x26 );
rtc_write( DS_REG_DAY_WEEK, 0x06 );
rtc_write( DS_REG_HOUR, 0x08 );      /* при B.24/12 = 0: 1..12 + бит 0x80 = PM */
rtc_write( DS_REG_MIN, 0x56 );
rtc_write( DS_REG_SEC, 0x37 );
rtc_write( DS_REG_B, (uint8_t)( rtc_read( DS_REG_B ) & ~DS_B_SET ) ); /* загрузить и запустить */

rtc_write( DS_REG_D, 0x00 );         /* «время недостоверно»: следующий старт поставит заново */
rtc_write( DS_REG_D, DS_D_VRT );     /* наоборот — пометить время годным */
```

NVRAM (`0x0E…0x3F`) — обычная запись/чтение байта, значение живёт в BKP:

```c
rtc_write( 0x0E, 0x5A );             /* сохранится при выключении питания (VBAT) */
uint8_t v = rtc_read( 0x0E );
```

## 6. Точность и питание

* Часы идут от `LSE` (кварц 32.768 кГц): уход определяется кварцем (обычно единицы ppm,
  секунды в сутки). При отсутствии LSE — `LSI` (~40 кГц), уход может быть десятки секунд в сутки.
* Счётчик, настройки (`DR26`), метка/`VRT` (`DR27`) и NVRAM (`DR1…DR25`) питаются от `VBAT`:
  при снятии основного питания время и содержимое NVRAM сохраняются.
* При потере `VBAT` пропадает метка `0x5A` → при следующем включении время ставится заново
  (см. §4), а NVRAM обнуляется.

## 7. Проверочный чек-лист

1. Первый запуск: строки `RTC: no valid time…`/`SET=1`/`time set to 26.09.2026 08:56:37`/`VRT=1`
   и дамп `37 00 56 00 08 00 06 26 09 26 | A=20 B=02 C=00 D=80`.
2. Секунды растут: bring-up задача печатает `[RTC] мм.ЧЧ.ГГ чч:мм:сс dow=… A=… B=… C=… D=…`
   каждые ~20 с (значения — «сырые» байты регистров, по умолчанию BCD).
3. Повторный сброс/перепрошивка без отключения VBAT: `RTC: clock=LSE, magic=1, enabled=1, cnt=…,
   VRT=1` — время **не** переустанавливается и продолжает идти.
4. Запись в `0x0E…0x3F` возвращается после сброса (NVRAM в BKP).
5. Запись `0x0D = 0x00` → при следующем сбросе время снова ставится по умолчанию.
6. Независимая проверка без UART: `RTC->CNT` (по адресу `0x40002800`) равен `1790412997 +`
   прошедшие секунды, в BKP (`0x40006C00 + 0x7C…`) видны `DR26/DR27/DR28…`.

## 8. Что осталось за рамками

* alarm-регистры (`0x01/0x03/0x05`) — чтение `0x00`, запись игнорируется;
* прерывания часов (`PIE/AIE/UIE`) сохраняются в регистре `B`, но IRQ не генерируются
  (флаг `UF` в регистре `C` доступен программно);
* квадратная волна (`SQWE`) не формируется;
* 12/24-часовой режим реализован кодированием бита `0x80` (PM) в регистре часов;
* год — 2 цифры (век 20xx); день недели — производное от даты (`1 = Пн … 7 = Вс`);
* функции `rtc_read/rtc_write` не реентерабельны (общий доступ к RTC/BKP): при вызове из
  нескольких задач их нужно защищать мьютексом.

